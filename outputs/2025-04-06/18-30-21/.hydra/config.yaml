model:
  model_source: local
  local_name: ${hydra:runtime.cwd}/../LLMs/mistral-7b-bnb-4bit/
  remote_name: ''
  max_seq_length: 512
  dtype: float16
  load_in_4bit: false
per_device_train_batch_size: 4
gradient_accumulation_steps: 2
learning_rate: 2.0e-05
num_epochs: 3
lr_scheduler_type: cosine
optim: adamw_hf
loss_type: vanilla
dataset_num_proc: 1
csv_out: ./outputs/test_predictions.csv
alpaca_prompt: 'Below is an instruction...

  Instruction: {}

  Input: {}

  Response:

  {}'
instruction: Please reconstruct the crystal structure.
output_prompt: Generate atomic structure description with lattice lengths, angles,
  coordinates and atom types.
id_prop_path: ${hydra:runtime.cwd}/data/processed/id_prop.csv
output_dir: ${hydra:runtime.cwd}/outputs
model_save_path: ${hydra:runtime.cwd}/outputs/my_model
prop: tc_ad
chem_info: formula
id_tag: id
model_source: local
num_train: 100
num_test: 20
seed_val: 42
